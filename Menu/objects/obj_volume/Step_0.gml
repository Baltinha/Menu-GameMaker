//Aqui ira configurar o volume do som de acordo com o escolhido na hud
/*if(volumeAtual != volume)
{
	volumeAtual = volume;
	var num = audio_get_listener_count()
	for(var i =0; i < num; i++)
	{
		var info = audio_get_listener_info(i);
		var ind = info[? "index"];
		audio_set_master_gain(info[? "index"],volumeAtual/10);
		ds_map_destroy(info);
		show_debug_message("volume:" + string(ind));
	}

}*/
